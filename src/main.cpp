#include "data.hpp"
#include "neural_network.hpp"
#include <iostream>

NeuralNetwork NN;

int main() {
    // TODO: read in weigts and biases (possible to do at compile time and not runtime?
    )
    NN.layers = 
    std::cout << "test" << std::endl;    
    return 0;
}